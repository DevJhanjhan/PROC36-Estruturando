//Definindo variáveis
var canvas, backgroundImage, brgImg, database, form, player, playerCount;

function preload(){

}

function setup(){

}

function draw(){

}

//Ajustando a altura e largura automaticamente sempre que o tamanho da janela é alterado
function windowResized() {
    resizeCanvas(windowWidt,windowHeight);
}